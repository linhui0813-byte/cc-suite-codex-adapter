# Repository instructions

- `plugins/cc-suite-codex/` and `.agents/plugins/marketplace.json` are generated.
- Change `adapter.toml`, `overlay/`, or `scripts/`; then run `python3 scripts/sync_upstream.py`.
- Never edit or activate the upstream Claude installation from this repository.
- Keep upstream pins immutable and preserve the ISC license and attribution.

